# Telegram Growth & Intelligence Platform — Phase 1 (Data Foundation) dependencies
# Only libraries required by the ingestion + storage layer are listed here.
# Intelligence / NLP / dashboard dependencies are intentionally NOT added yet
# (strict build order: DATA -> STRUCTURE -> INTELLIGENCE -> ... -> UI).

# --- Telegram ingestion (own channels: full history + stats via MTProto) ---
telethon==1.36.0

# --- HTTP (competitor t.me/s scraping + merchant collectors) ---
httpx==0.27.2
h2>=4.1.0                   # httpx[http2] extra — required for httpx.AsyncClient(http2=True)
beautifulsoup4==4.12.3      # pure-Python HTML parsing for t.me/s preview pages

# --- Domain parsing (registrable-domain extraction for link resolution) ---
tldextract>=5.1.2           # correct eTLD+1 handling (.co.in, .co.uk, etc.)

# --- Database / ORM (SQLite locally, Postgres-portable) ---
# >=2.0.43 required for Python 3.14 typing compatibility.
SQLAlchemy>=2.0.43
alembic>=1.14.0            # migrations

# --- Scheduling ---
APScheduler>=3.10.4

# --- Config ---
python-dotenv>=1.0.1

# --- CLI + logging ---
# >=0.15.1 required for Click 8.2+ compatibility.
typer>=0.15.1
rich>=13.9.4

# --- AI layer (provider-selectable: openai reasoning | groq) ---
openai>=1.50.0
groq>=1.5.0

# --- Postgres driver (optional; only needed if DB_URL points at Postgres) ---
# psycopg[binary]==3.2.3

# --- Web search (free, no API key) for competitor platform detection ---
duckduckgo_search>=7.5.1

# --- Web API + server ---
fastapi>=0.115.0
uvicorn>=0.30.0

# --- Deal scraping (Cloudflare-bypassing stealth browser) ---
# After install run: python -m camoufox fetch   (downloads the browser binary)
camoufox>=0.4.11
playwright>=1.61.0
