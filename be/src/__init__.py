"""DealWing backend (FastAPI) — package root.

Layered layout (Full-Stack Python Template): routers → controllers → services.
Run the API with ``uvicorn src.main:app`` and the CLI with ``python -m src.cli``.
"""

__version__ = "1.0.0"
__phase__ = "Full stack (fe/ + be/)"
