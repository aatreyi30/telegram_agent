"""Metrics layer — raw aggregation of stored facts into per-entity metrics.

Intelligence engines (Phase 4+) CONSUME these; they do not compute raw metrics
themselves (README/13 separation of concerns).
"""
