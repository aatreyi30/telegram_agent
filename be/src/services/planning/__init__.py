"""Campaign & Planning Engine (Phase 10).

Converts approved intelligence into structured execution plans (daily / weekly /
event). Creates plans only — never captions, never publishing, never metrics.
"""
