# Base URL of the backend API. Leave empty for same-origin (backend serves the
# built SPA) or when using the Vite dev proxy. Set it when the frontend is hosted
# separately from the backend, e.g. http://localhost:8000
VITE_API_URL=
